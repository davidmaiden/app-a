import React from 'react';
import { Typography } from '@material-ui/core';

export default class Third extends React.Component {
    render() {

        return (
            <>
                <Typography variant='h2'>Third Page</Typography>
            </>
        );
    }
}